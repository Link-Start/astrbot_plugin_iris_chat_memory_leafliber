# 向量索引
faiss-cpu>=1.7.0
numpy>=1.24.0

# 嵌入模型（可选，仅当 l2_memory.embedding_source 为 local 时需要）
# sentence-transformers>=2.2.0

# Token 计数
tiktoken>=0.5.0

# Web 框架（阶段11）
quart>=0.19.0

# JWT 认证（阶段11）
PyJWT>=2.8.0

# 图片处理（pHash去重 + 无效图过滤）
Pillow>=10.0.0